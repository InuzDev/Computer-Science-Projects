/*
 * Programming fundamentals - A. Liz practice notesheet exercise examples
 * Charles David - File ; Index.c
 *
 * This file is only purpose is to propuse a template of a empty project,
 * explaining their parts and how it should be created
 */

#include <stdio.h>

// always use macros to define constants. So when you need to change the value
// of a conditional, loop or anything else that contains the macro.
#define EXAMPLE_MACRO 9999
//      Remember to use a descriptive names.

// Always create prototype functions, to avoid
int main() {

   printf("Test hello world");
   return 0;
}
